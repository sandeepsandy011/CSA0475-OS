#include <stdio.h>
#include <unistd.h>

int main()
{
    int p_id, p_pid;
    p_id = getpid(); 
    p_pid = getpid(); 
    printf("Process ID: %d\n", p_id);
    printf("Parent Process ID: %d\n", p_pid);

    return 0;
}
OUTPUT:
Process ID: 19392
Parent Process ID: 19392
